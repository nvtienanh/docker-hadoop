sudo: required

services:
   - docker

before_deploy:
   - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"

deploy:
   - provider: script
     script: ./build.sh
     on:
        branch: $TRAVIS_BRANCH